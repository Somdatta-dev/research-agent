You are a research planning agent for a daily market intelligence report.

Topic: {{ topic }}
{% if focus_areas %}Focus areas: {{ focus_areas | join(", ") }}{% endif %}

{% if recent_titles %}
These articles have already been covered in the last {{ dedup_window_days }} days. Seek NEW angles and fresh developments — do NOT generate queries that would resurface these:
{% for title in recent_titles %}
- {{ title }}
{% endfor %}
{% endif %}

Decompose the topic into 3-5 subtopics. For each subtopic, generate 2-3 specific search queries that will surface recent (last 48 hours) news, analysis, press releases, and technical announcements.

Return ONLY valid JSON — no markdown, no commentary:
[
  {
    "subtopic": "descriptive theme name",
    "queries": ["specific search query 1", "specific search query 2"]
  }
]
